---
import PointSpan from "@components/commons/PointSpan.astro";
import LeftLine from "@components/commons/LeftLine.astro";
import { cn } from "@lib/utils";

const skills = [
  [
    "Frontend",
    ["React", 1],
    ["Nextjs", 1],
    ["Tailwind", 1],
    ["Tanstack-Query", 1],
    ["zustand", 1],
  ],
  ["Backend", ["Node", 1], ["Nestjs", 2], ["SQLight", 2]],
  ["Infra", ["Ubuntu", 2], ["Docker", 2], ["Nginx", 3], ["Proxmox VE", 3]],
  ["ETC", ["Git/Github", 1], ["Figma", 1], ["Notion", 1]],
];
---

<div class="my-12 flex flex-col gap-8">
  <div class="flex flex-wrap justify-between gap-3">
    <h2>
      <PointSpan text="Skills" className="text-5xl font-bold" id="skills" />
    </h2>
    <div class="flex justify-end gap-2">
      <li class="list-none text-sm">
        <ul class="flex items-center gap-2">
          <div
            class="flex h-4 w-4 items-center justify-center rounded-full border border-[#0048fd] bg-[#0048fd] text-xs font-semibold text-white"
          >
            1
          </div>
          <div>프로젝트를 진행할 수 있어요</div>
        </ul>
        <ul class="flex items-center gap-2">
          <div
            class="flex h-4 w-4 items-center justify-center rounded-full border border-[#0048fd] bg-[#80a3fa] text-xs font-semibold text-white"
          >
            2
          </div>
          <div>어느정도 사용할 줄 알아요</div>
        </ul>
        <ul class="flex items-center gap-2">
          <div
            class="flex h-4 w-4 items-center justify-center rounded-full border border-[#0048fd] bg-white text-xs font-semibold text-[#0048fd] dark:bg-[#0048fd]/20"
          >
            3
          </div>
          <div>보고 이해할 수 있어요</div>
        </ul>
      </li>
    </div>
  </div>
  <LeftLine>
    <div class="flex flex-col gap-3">
      {
        skills.map(([skill, ...stacks]) => (
          <div class="flex gap-4 max-[420px]:flex-col">
            <h3 class="min-w-[120px] text-right text-2xl font-semibold max-[420px]:text-center">
              {skill}
            </h3>
            <li class="flex flex-1 list-none flex-wrap gap-4 max-[420px]:justify-center">
              {stacks.map(([stack, level]) => (
                <ul class="flex items-center gap-1">
                  <div
                    class={cn(
                      "flex h-5 w-5 items-center justify-center rounded-full border border-[#0048fd] bg-[#0048fd] text-xs font-semibold text-white",
                      {
                        "bg-white text-[#0048fd] dark:bg-[#0048fd]/20":
                          level === 3,
                        "bg-[#80a3fa]": level === 2,
                        "": level === 1,
                      },
                    )}
                  >
                    {level}
                  </div>
                  <div class="text-base">{stack}</div>
                </ul>
              ))}
            </li>
          </div>
        ))
      }
    </div>
  </LeftLine>
</div>
